# Archive — Superseded Label Artifacts

`gold_dataset_binary.csv` is a SUPERSEDED pilot artifact from an early domain-agnostic ensemble
(GPT-4 / Gemini 1.5 / Meta AI, 3-way HIGH/MID/LOW). It is NOT the evaluation set.

The canonical 60-transcript eval set is `data/labels/test_set_final.csv` (28 HIGH / 32 LOW).
